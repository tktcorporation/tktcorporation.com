{
  "mcpServers": {
    "lapras": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@lapras-inc/lapras-mcp-server"],
      "env": {
        "LAPRAS_API_KEY": "${LAPRAS_API_KEY}"
      }
    }
  }
}
